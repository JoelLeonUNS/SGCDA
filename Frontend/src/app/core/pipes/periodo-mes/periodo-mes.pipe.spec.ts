import { PeriodoMesPipe } from './periodo-mes.pipe';

describe('PeriodoMesPipe', () => {
  it('create an instance', () => {
    const pipe = new PeriodoMesPipe();
    expect(pipe).toBeTruthy();
  });
});
